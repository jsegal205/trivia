import React from "react";
import ReactDOM from "react-dom/client";

const App: React.FC = () => <main>Helllooo react</main>;

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <App />
);
